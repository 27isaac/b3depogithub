# b3depogithub
  POURQUOI : On va créer un fichier README.md directement sur l'interface GitHub (sans passer par Git Bash). Puis on va le 'tirer' (pull) sur notre machine locale pour synchroniser. C'est le scénario typique de collaboration : quelqu'un modifie le repo sur GitHub, et on récupère ses changements avec git pull.
